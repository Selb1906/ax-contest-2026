# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

**2026년도 AX 아이디어 경진대회 — 데이터 분석 / 지정 분석(12개) 부문 / 한국전력공사 제시 과제**

- 과제명: **전기요금 과다발생 사전 예측모델 개발**
- 원본 과제 요구사항: `지정과제(한국전력공사).pdf` (텍스트 추출본: `_task_brief.txt`)
- 제출 양식: `2026년도 AX 아이디어 경진대회_데이터 분석_지정 분석(12개) 부문_양식 및 동의서/` 하위 `.hwp` 4종 (보고서 양식, 참가 서약서, 개인정보 동의서, 저작재산권 동의서)

현 시점 워크스페이스는 **코드 없음 / 과제 브리프 + 제출 양식만 존재** 하는 상태다. 실제 코드는 아래 "개발 워크플로" 제약에 따라 데이터안심구역 방문 이후에 이 저장소에 쌓이기 시작한다.

## 과제 핵심 요구사항 (요약)

### 예측 대상
- 고객별 **기준일(검침일) +10일, +20일 시점의 기준월 전력사용량** 예측
- 학습 기간: 직전 1~3년
- 입력: **15분 단위 LP(Load Profile) 데이터** — 유효전력량, 무효전력량, 최대수요전력 등

### 알림 조건 (과다발생 판정)
세 가지 중 **한 건이라도** 만족하면 알림 대상:
- 전월 대비 **+30% 초과**
- 전년 동월 대비 **+30% 초과**
- 직전 3개월 평균 대비 **+50% 초과**

### 데이터 (안심구역 내에서만 제공)
- 계약종별 일반용 고객 약 **3,000호**
- 주택용 고객 약 **500호**
- 최대 **3년치, 15분 간격** LP 데이터

### 제출물
1. 분석 결과 보고서 (제공된 `.hwp` 양식 사용)
2. 분석 코드 (Python 스크립트 등)
3. 근거 데이터 (사용한 외부/공공 데이터는 **출처 표기 필수**)

### 평가 기준 (공식)
- **예측 사용량 vs 실제 전력사용량의 차이** (정확도)
- **스케일 아웃을 고려한 코드 설계의 논리적 정합성** (단순 노트북 실험이 아니라, 수만~수십만 호로 확장 가능한 설계인가)

## 개발 워크플로 (중요 제약)

이 과제는 **일반적인 로컬 개발이 불가능**하다. 반드시 아래 제약을 전제로 작업해야 한다.

- 원본 LP 데이터는 **데이터안심구역 외부 반출 불가**
  - 서울: 서초구 효령로72길 60, 한전아트센터 4층
  - 나주: 전력로 55, 한전 본사 2층
  - 방문 전 **별도 이용 신청** 및 방문일정 협의 필요
- **반출 가능한 것**: 분석 코드, 결과물(지표/차트/보고서)
- **반입 가능한 것**: 직접 수집한 외부 데이터(예: 기상청 데이터) — 사전 협의/반입 절차 필요
- 분석 툴: **Python 3.9 이상** (안심구역에서 제공되는 분석 환경 사용)

### 이 저장소의 역할 분리
- 본 로컬 저장소(`E:\AX_Contest`):
  - 과제 이해, 접근 방식 설계, **실제 데이터 없이 돌릴 수 있는 코드 스켈레톤**
  - 공개 데이터(기상청 등) 수집/전처리 스크립트
  - 합성 데이터로 검증하는 단위 테스트
  - 보고서 초안
- 데이터안심구역 (방문 시):
  - 위 코드를 반입해 실제 LP 데이터로 학습·평가
  - 결과 지표/차트만 반출 → 이 저장소의 보고서에 반영

이 분리를 전제로, 로컬에 작성하는 코드는 "실제 LP 데이터 경로를 외부 config로 주입하면 그대로 동작"하는 구조여야 한다. 데이터 경로 하드코딩 금지.

## 공공데이터 후보

- 과제 적합도 기준으로 정리된 Tier S/A/B/C 리스트: `data_candidates.md`.
- **실제 다운로드된 Tier S 3건의 스키마·샘플 검증 결과**: `data_schemas.md` (data.go.kr 다운로드 자동화 2-step 호출 방식도 문서화됨).
- 로컬 실데이터는 `data/raw/` 에 저장 (git ignore 대상).

요약:
- 한전 **공동주택 전력·기상 융합데이터 (15120804)** — 2020·2021·2022 **3개년** 격자 단위 시간별 **전력+기상 결합**. 기상→전력 모델 사전학습 후보.
- 지역난방공사 **(합성) AMI 15149950/15149952** — 15분 단위·고객ID 없음·약 3개월. 파이프라인 스모크 테스트용.
- 기상 공변량은 지역난방공사 **외기온도/기상실측/예측** 을 1차로 쓰되 커버리지 부족 시 **기상청 API Hub** (apihub.kma.go.kr) 병행.

## 접근 방향 초안 (Draft)

아래는 합의된 설계가 아니라 **출발점용 스케치**다. 진행하면서 사용자와 재검토 필요.

### 알려진 한계 (TODO)

- **합성 LP v1 의 "전월 대비 +30% 초과" 알림 base rate 가 0%**. 원인: 현재 생성기가 15분 단위 잡음만 있어 월 합계 수준에선 평균화로 소멸. 실 LP 의 월간 변동은 **고객×월 단위 잡음**(여행·이사·가전 추가 등)에서 발생. 해결: `src/synth.py` 의 `_simulate_segment` 에 고객별 월 multiplier 레이어를 추가 필요 (예: `monthly_mult ~ LogNormal(0, 0.2)`).  
  - 현재 v1 상태: cond_ma3 3.6% (공공 3.1% 와 유사) / cond_prev 0% (공공 8.2% 대비 미달)
  - 스카우팅 방문에서 실 LP 의 **월 편차 σ** 를 통계로 뽑아오면 그 값으로 직접 보정 가능

### 모델링
- 베이스라인: 작년 동월 naive / 3개월 이동평균 / seasonal naive
  - 평가 기준이 "차이"이므로 강한 베이스라인부터 설정하고 상회 여부를 먼저 본다
- 후보 모델: 
  - 고객별 개별 모델 (Prophet, SARIMAX) — 해석 용이
  - 글로벌 모델 (LightGBM/XGBoost + 고객 ID 임베딩, 혹은 N-HiTS/TFT) — 3,500호 대상이라면 글로벌이 일반적으로 유리
- 피처:
  - 15분 → 일/주 단위 집계 + 일내 패턴(피크 시간대, 주말/평일)
  - 계절성: 월, 요일, 공휴일
  - **기상 공변량**: 기온(HDD/CDD), 습도 — 외부 수집 후 안심구역 반입 필요
  - 고객 프로파일(계약종별 일반/주택용) 원핫
- 예측 시점 설계:
  - `+10일`은 이미 기준월의 약 1/3이 실측된 상태 → **부분 실측 + 잔여 기간 예측** 구조 (혼합 모델: 실측분은 합산, 미래분만 모델 예측)
  - `+20일`은 약 2/3 실측 — 동일 구조, 잔여분 짧음 → 정확도 자연히 향상
- 알림 로직은 예측된 월 사용량을 기준으로 3개 조건을 독립 평가

### 스케일 아웃 설계 (평가 가점 포인트)
- 피처 파이프라인은 **고객 축으로 병렬화 가능**한 배치 잡 구조 (pandas 단일 프로세스 말고 Dask/Polars/Spark 중 택1)
- 모델 학습·추론 분리: 학습은 오프라인 주 1회, 추론은 검침일 주기로 일일 배치
- 코드 레이어:
  - `io/`: 데이터 로딩 (LP 원본, 기상, 캘린더) — 인터페이스는 경로만 받고 내부 구현 스왑 가능
  - `features/`: 집계·파생 피처 (순수 함수, 테스트 용이)
  - `models/`: baseline / global / per-customer 각각 동일 인터페이스(`fit`, `predict`)
  - `eval/`: 지표(MAPE/MAE/sMAPE), +10/+20 horizon별 집계
  - `serve/`: 알림 판정 (예측 + 3개 조건 → flag)
  - `configs/`: YAML (데이터 경로, 하이퍼파라미터)
- 재현성: 랜덤 시드 고정, `requirements.txt` pin, 결과물에 학습 기간·데이터 버전 기록

### 보고서 구성 초안
1. 과제 이해 및 문제 정의 (알림 조건 명시)
2. 데이터 특성 분석 (EDA 결과)
3. 모델 접근: 베이스라인 → 제안 모델
4. 평가 결과: horizon별 정확도, 알림 정밀도/재현율
5. 스케일 아웃 설계 근거
6. 활용 시나리오 (과제 원문의 "고객 합리적 소비 유도")
7. 한계 및 향후 개선

## 명령 / 도구 노트

- PDF 텍스트 추출 (PyMuPDF 설치 기준): 
  ```bash
  python -c "import fitz; print('\n'.join(p.get_text() for p in fitz.open('지정과제(한국전력공사).pdf')))"
  ```
  — 콘솔 한글 깨짐 방지 위해 파일로 리다이렉트 권장. 추출 결과는 이미 `_task_brief.txt`로 저장되어 있다.
- `.hwp` 양식은 한컴오피스에서만 편집. 파이썬으로 내용 파싱이 필요하면 `olefile` 기반 라이브러리 고려.
- 이 저장소는 git 저장소가 아니다. 본격 개발 시작 시 `git init` 여부는 사용자와 상의 후 결정 (제출 규정상 반출 가능한 산출물에 `.git`을 포함할지 검토 필요).

## 작업 시 주의

- 데이터 샘플, 고객 ID, 실제 LP 값 등은 **이 저장소 또는 외부로 절대 유출 금지**. 로컬 프로토타이핑은 합성 데이터만 사용.
- 공개 데이터(기상청 등) 사용 시 **출처 명시**는 제출 요건이다. 수집 스크립트에 URL/다운로드 일자 주석 의무.
- "활용계획" 항목에 따라 제안 기관(한전)이 코드/아이디어 활용을 요청할 수 있음 — 코드 가독성·문서화는 평가 외적으로도 중요.
